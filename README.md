## How to run

1. Go to config.yaml, set the name of experiment in the name sections
2. change the desired parameters and save
3.  run on command line - 'python run_experiment.py --config config.yaml'
